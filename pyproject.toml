[project]
name = "email-agent"
version = "0.2.0"
description = "AI email assistant — manages multiple Gmail inboxes via Slack DMs"
requires-python = ">=3.10"
license = {text = "MIT"}

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.pyrefly]
python-version = "3.10.0"
